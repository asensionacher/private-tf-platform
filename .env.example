# ==============================================================================
# IAC Platform - Simple Configuration
# ==============================================================================
# Just set these 4 variables and you're ready to go!
# ==============================================================================

# 1. SECURITY - Change these for production!
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=change-this-to-a-secure-32-char-key!
POSTGRES_PASSWORD=change-this-password

# 2. DOMAIN - Your registry domain (e.g., registry.local, privatetf.local)
# This is used across all services (frontend, backend, runner)
REGISTRY_HOST=registry.local

# 3. PORTS (optional - defaults work for most cases)
FRONTEND_PORT=3000
BACKEND_PORT=9080

# ==============================================================================
# That's it! Run: docker-compose up -d
# ==============================================================================
# 
# Access the platform:
# - Web UI: http://localhost:3000
# - Backend API: http://localhost:9080
# 
# Configure Terraform CLI in ~/.terraformrc:
#   host "registry.local:9080" {
#     services = {
#       "providers.v1" = "http://registry.local:9080/v1/providers/"
#       "modules.v1"   = "http://registry.local:9080/v1/modules/"
#     }
#   }
#   credentials "registry.local:9080" {
#     token = "<generate-api-key-from-ui>"
#   }
# 
# ==============================================================================

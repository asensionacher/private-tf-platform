# Runner Environment Variables

# Allowed origins for CORS (comma-separated)
# Use * to allow all origins (development only)
# In production, specify exact origins
# Example: http://localhost:3000,https://app.example.com
ALLOWED_ORIGINS=*

# Port where the runner listens
# Default: 8080
PORT=8080

# Temporary directory for deployments
# Default: /tmp/iac-deployments
DEPLOYMENTS_DIR=/tmp/iac-deployments

# Default timeout for deployments (in minutes)
# Default: 60
DEFAULT_TIMEOUT=60

# Private Terraform/OpenTofu Registry URL
# URL of the backend registry for modules and providers
# Must use hostname with dot for Terraform compatibility
# Default: http://registry.local:9080
REGISTRY_HOST=http://registry.local:9080

# Encryption key for securing sensitive data (SSH keys, passwords, tokens)
# IMPORTANT: Change this to a strong, randomly generated 32-character key in production
# You can generate one with: openssl rand -base64 32
ENCRYPTION_KEY=change-this-to-a-secure-32-char-key!

# Runner URL - Internal network URL for backend to communicate with runner
RUNNER_URL=http://runner:8080

# Allowed CORS origins for runner (comma-separated or *)
ALLOWED_ORIGINS=*

# Private Terraform/OpenTofu Registry URL - Internal network URL for runner to access modules/providers
# Must use hostname with dot for Terraform compatibility
REGISTRY_URL=http://registry.local:9080

# Frontend API base URL (leave empty to use same origin)
# Only needed if frontend and backend are on different domains/ports
VITE_API_BASE_URL=

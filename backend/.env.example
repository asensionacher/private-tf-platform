# Backend Environment Variables

# Host configuration (used for CORS and URL generation)
FRONTEND_HOST=localhost
BACKEND_HOST=localhost
REGISTRY_HOST=localhost

# Port configuration
# Frontend web interface port (for CORS configuration)
FRONTEND_PORT=3000

# Vite development server port (for CORS during frontend development)
VITE_DEV_PORT=5173

# Backend API port
PORT=9080

# Runner URL for executing deployments
# Default: http://runner:8080
RUNNER_URL=http://runner:8080

# Encryption key for sensitive data (git auth, etc.)
# Generate a secure key with: ./generate-encryption-key.sh
ENCRYPTION_KEY=your-32-character-encryption-key-here

# PostgreSQL Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=iacplatform
POSTGRES_USER=iacuser
POSTGRES_PASSWORD=iacpassword
